# 車両・配送管理システム（Supabase版）セットアップ手順

現行のGoogle Apps Script（GAS）＋スプレッドシート構成から、Supabase（Postgres）に置き換えた新しいアプリです。
デプロイ方法は今までと同じ「Vercelに静的ファイルを置くだけ」で、ビルド作業は不要です。

## 何が変わったか（要約）

- **入力先がスプレッドシートではなくアプリ内**になりました（会社/車両/ドライバー/日々の運行/月次点検/配送実績、すべてアプリ上で入力・編集できます）。
- **速度**：これまでは1回の保存のたびに5シート全件をGAS経由で読み直していたのが最大のボトルネックでした。Supabaseでは、必要な期間・必要なテーブルだけを取得し、複数件の保存も1回の通信でまとめて送るようにしています。また他のスタッフの更新が自動で画面に反映されます（リアルタイム同期）。
- **予定/実績の把握**：新しく「実績一覧」タブを追加しました。期間・取引先・車両で絞り込み、その期間に運行予定だった便のうち「まだ記録されていないもの」を一目で確認できます（赤色でハイライト）。
- **入力導線**：「便を追加」モーダルが「本日」タブからも直接開けるようになりました（これまではカレンダーの日付詳細を開かないと追加できませんでした）。日付は複数選択可能（同じ内容を複数日にまとめて登録できる）、納品/引取の種別（📦/🔄）とメモも設定できます。写真はカメラ撮影用ボタンとギャラリー選択用ボタンを分けたので、スマホでの操作がスムーズになりました。過去日の配送実績も後から記録・編集できます。
- **カレンダーの日付詳細**を車両ごとにグループ表示するようにしました。どの車が何件回るかが一目でわかります。
- マスタ（取引先・車両・ドライバー）の登録/修正/削除は、通信が終わる前に画面へ即座に反映されるようにしました（体感速度の向上）。
- **ログイン**：社内共有の1アカウントでログインする方式にしました（外部からの誤アクセスを防ぎつつ、個別アカウント管理の手間は増やさない設計です）。

写真の保存先は今までどおりCloudinaryのままです。

---

## セットアップ手順

### 1. Supabaseプロジェクトを作成

1. https://supabase.com にアクセスし、無料プランでプロジェクトを新規作成します。
2. プロジェクト作成後、左メニューの **SQL Editor** を開き、同梱の `schema.sql` の中身を貼り付けて実行します。
   - これでテーブル一式（vehicles / drivers / companies / dispatch_days / checks / delivery_records）と、アクセス制御（RLS）、リアルタイム配信の設定が完了します。

### 2. ログイン用の共有アカウントを作成

1. 左メニューの **Authentication → Users** を開き、「Add user」から社内共有のメールアドレスとパスワードを1つ作成します（例: dispatch@yourcompany.example）。
2. **Authentication → Providers** で Email 認証が有効になっていることを確認してください（デフォルトで有効です）。
3. このメールアドレス・パスワードを、実際に使う5人に共有してください。

### 3. 接続情報を取得して `config.js` に設定

1. 左メニューの **Settings → API** を開きます。
2. `Project URL` を `config.js` の `SUPABASE_URL` に、`anon public` キーを `SUPABASE_ANON_KEY` に貼り付けます。

```js
const SUPABASE_URL = 'https://xxxxxxxx.supabase.co';
const SUPABASE_ANON_KEY = 'ey................';
```

Cloudinaryの値（`CLOUDINARY_CLOUD` / `CLOUDINARY_PRESET`）は今までと同じ設定を使うのであればそのままで大丈夫です。

### 4. 既存データの移行（任意）

これまでのスプレッドシートのデータを引き継ぎたい場合は `migrate.js` を使います。

1. 現行スプレッドシートの各タブを「ファイル → ダウンロード → カンマ区切り値(.csv)」でそれぞれ書き出し、`vehicles.csv` / `drivers.csv` / `routes.csv` / `checks.csv` / `delivery_records.csv` という名前で `migrate.js` と同じフォルダに置きます。
2. Supabaseの **Settings → API** から `service_role` キー（anonキーとは別物です。外部に漏らさないよう注意してください）を取得します。
3. まずはドライラン（試し実行、実際の書き込みなし）で内容を確認します。

```bash
node migrate.js --dry-run
```

4. 出力される変換後データを確認し、問題なければ本実行します。

```bash
SUPABASE_URL="https://xxxx.supabase.co" \
SUPABASE_SERVICE_ROLE_KEY="xxxxxxxx" \
node migrate.js
```

> ⚠️ **重要**：`migrate.js` はGAS側の実際のコードを見ずに、アプリのフロントエンドの通信内容から列構成を推測して作っています。Apps Scriptのコードを共有していただければ、実際のシート構成に合わせて内容を確認・調整します。ズレがあった場合はCSVのヘッダー行（1行目の列名）を `migrate.js` 冒頭の `CSV_SCHEMAS` に合わせて書き換えれば動くはずです。

### 5. アプリ本体をデプロイ

今までどおりGitHubリポジトリ（`saiki-028D/teikibinn`）にファイルを置き、Vercelで公開する形で問題ありません。

1. `index.html` / `app.js` / `config.js` をリポジトリに追加（またはこれまでの `index.html` を置き換え）してコミット・プッシュします。
2. Vercel側の設定は変更不要です（静的ファイルなので、これまでと同じビルド設定のまま自動デプロイされます）。

---

## 運用メモ

- **月次点検の保存**：これまでは項目ごとに何度も通信していましたが、1回の保存で26項目まとめて送るようにしたので、点検結果の保存が速くなっています。
- **本日の運行日程の確定**：チェックした便をまとめて1回の通信で送るようにしています（会社数が多くても保存が速いです）。
- **RLS（アクセス制御）**：現状はログインさえしていれば全員が全データを読み書きできる、シンプルな設定にしています。将来的に「入力担当者ごとに権限を分けたい」などの要望が出てきたら、テーブルごとのポリシーを調整することで対応できます。
- **バックアップ**：Supabaseは自動バックアップがありますが、無料プランは保持期間が短いです。データが増えてきたら有料プランの検討、または定期的なCSVエクスポートをおすすめします。

## 補足

- `migrate.js` の列構成は、実際のGAS（Apps Script）コード（`doGet`/`doPost`/`saveDelivery`/`upsertRow`/`saveCheck`）を確認したうえで作成しています。想定どおりであれば `checks.csv` は1項目=1行、`delivery_records.csv` の写真URLはカンマ区切り文字列という前提が正しいことを確認済みです。それでも実データでの本実行前には、必ず `--dry-run` の出力を目視確認してください。
- ログイン方式は「共有1アカウント」で実装しています。将来的に個別アカウント・個別権限が必要になった場合はご相談ください。
